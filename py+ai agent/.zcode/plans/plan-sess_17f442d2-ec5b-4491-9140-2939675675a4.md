# Obsidian Vault 图片资产重构 — 执行方案

## 现状扫描结果
- **图片总数**：52 张（磁盘）· **被引用**：48 张 · **孤立**：5 张
- **Markdown**：15 个（其中 2 个空文件：`2026-07-11.md`、`Python 算数运算符.md`）
- **引用方式**：全部为 Obsidian 嵌入语法 `![[图片名]]`（部分带 `|宽度` 参数）
- **现有 photo/ 根目录**：空目录，可直接使用

## 关键发现（已逐张视觉分析确认）
| 类别 | 数量 | 处理方式 |
|------|------|----------|
| 被引用图片 | 47 张 | 移入 `photo/<md目录>/<md文件名>/`，按归属唯一归属 |
| 字节级重复（未引用） | 1 张 | **经你授权删除** `Pasted image 20260628143844.png`（与 143858 MD5 相同） |
| 孤立图片（无引用） | 4 张 | 移入 `photo/未归档/` |
| 失效引用（磁盘无文件） | 1 处 | `大模型认知和工程概览.md` 引用 `Pasted image 20260701141227.png` 不存在 → **保持原状，仅报告** |
| 多归属图片 | 2 张 | `Python_数据类型.md` 为归属（字典序先于 `python_day1.md`），同步更新 `python_day1.md` 引用 |

## 归属规则（按字典序的"首次引用"）
- `Python_数据类型.md` ← `字符串索引`、`字符串切片`（`python_day1.md` 也引用，改其引用）
- 其余 45 张均仅被一个 md 引用，归属唯一明确

## 目标目录结构（镜像 Markdown 层级）
```
photo/
├── LangChain/                      (20 张, owner: LangChain.md)
├── Python_数据类型/                (4 张, owner: Python_数据类型.md)
├── RAG/                            (1 张, owner: RAG.md)
├── ai agent/大模型认知和工程概览/   (17 张, owner: ai agent\大模型认知和工程概览.md)
├── 函数/                           (5 张, owner: 函数.md)
├── 提示词工程/                     (1 张, owner: 提示词工程.md)
└── 未归档/                         (4 张孤立: RAG与微调对比、Transformer的QKV机制、注意力机制公式、举例)
```

## 语义命名（已视觉确认 + 已精简至 4-10 字）
命名示例（按你确认的精简策略）：
- `大模型调用工具过程` → **工具调用流程**；`我们希望我们的agent拥有记忆` → **Agent记忆机制**
- `长期记忆vs短期记忆` → **长短期记忆对比**；`模型对提示开头结尾位置更敏感...` → **提示词位置敏感**
- `LangChain、LangGraph、DeepAgent关系` → **LangChain生态关系**
- 所有 `Pasted image xxx` 按内容重命名（如 `字符串索引`、`前馈神经网络`、`多头注意力机制`、`编码器解码器结构` 等）
- 低置信孤立图 `Pasted image 20260607082735.png` → **举例**（按你的指示）

**术语统一**：全文统一用 Agent / LangChain / RAG（不混用智能体/代理）；记忆类用"记忆"统一；注意力系列保持一致风格。

## 冲突处理
- 全部 52 个目标路径经校验**唯一无冲突**
- 同目录遇重名自动追加 `-2`、`-3`（本次实际未触发，但脚本内置）

## 引用更新策略
对每个 Markdown，把原 `![[原名.png|697]]` 替换为 `![[新名.png|697]]`（保留宽度参数与 `|`、`#` 修饰）。采用 Obsidian 标准 wiki embed 写法（仅文件名，无路径前缀），这与现有引用风格完全一致，Obsidian 会自动解析。

## 执行流程（幂等 · 分批 · 可回滚）
1. **预检**：再次扫描，若某图已在目标位置且命名正确 → **跳过**（保证幂等）
2. **创建目录**：`os.makedirs(..., exist_ok=True)` 共 7 个目录
3. **分批移动+重命名**（每批 ~50 张，本次 1 批即完成）：`shutil.move`，源不存在则跳过并记录
4. **删除重复**：`os.remove('Pasted image 20260628143844.png')`（仅此 1 个，经授权）
5. **更新引用**：对 6 个含图引用的 md 做正则替换，仅替换确实移动/改名了的图
6. **验证**：
   - 所有 `![[...]]` 引用的文件名在 `photo/` 下能找到 → 0 失效引用
   - 原始位置无遗留图片
   - 无孤儿图片（除 `photo/未归档/`）
   - **幂等测试**：再跑一次脚本 → 0 移动、0 重命名、0 引用修改
7. **清理**：删除临时脚本 `_build_plan.py`、`_plan.json`、`_refs_dump.json`

## 不修改项（安全优先）
- 不动 `.obsidian/`、`.git/`、`python/` 源码目录
- 不动 2 个空 md 文件、不删除任何 md
- 不动失效引用 `Pasted image 20260701141227.png`（仅报告）
- 不复制任何图片（每图唯一归属）

## 最终报告将包含
扫描数(52) · 已移动(47) · 已重命名 · 已修改引用(含 python_day1 的多归属同步) · 新建目录(7) · 未引用移入未归档(4) · 删除重复(1) · 自动编号(0) · 跳过(0) · 待人工确认(2: 失效引用 + 低置信命名) · 失败(0) · 总耗时